# OS / editor cruft
.DS_Store

# local tooling
.claude/

# large uncompressed backups (kept locally only)
archive/

# internal handoff / working notes
wild-healing-2026-handoff.md

# password-gate build: plaintext source + tooling stay local;
# only the built (gated) index.html is published
site-source.html
build-gate.py
wild-healing-2026-v10.html
